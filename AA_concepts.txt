add triangle circle collision

add hp
    animated hp loss

add dmgtype
    physical
        mass*delta_V
    energy/laser
        flatdps
    explosion
        flatdamage

adjust split():
    intersectpoint
    on split:
        +-(normalvektor+90° intersectpoint->this.pos * self.size - aMinRad/2)

add hud
    hp bar
    ammo
    fuel
    stat tab
    trajetory
        pos +

player movement
    pos <- pos + velocity*dt
    forward <- forward + angular_velocity*dt
    velocity <- velocity + acceleration*forward*dt

    pos + velocity * dt

    mx² + nx + b
    pos = pos + (velocity + acceleration*(forward + angular_velocity*dt)*dt)*dt
    
    dt -> 0 - 3


asteroidspawn seedrng